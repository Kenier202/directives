import { SharedDirective } from './shared/directives/custom-label.directive';

describe('SharedDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDirective();
    expect(directive).toBeTruthy();
  });
});
